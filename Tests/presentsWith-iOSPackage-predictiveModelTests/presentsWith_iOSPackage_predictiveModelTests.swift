import XCTest
@testable import presentsWith_iOSPackage_predictiveModel

final class presentsWith_iOSPackage_predictiveModelTests: XCTestCase {
    func testExample() {
        // This is an example of a functional test case.
        // Use XCTAssert and related functions to verify your tests produce the correct
        // results.
        XCTAssertEqual(presentsWith_iOSPackage_predictiveModel().text, "Hello, World!")
    }

    static var allTests = [
        ("testExample", testExample),
    ]
}
